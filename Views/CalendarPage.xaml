<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HybridTrackerApp.Views.CalendarPage"
             Title="Office Attendance Calendar"
             xmlns:controls="clr-namespace:Plugin.Maui.Calendar.Controls;assembly=Plugin.Maui.Calendar" >
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="calendarLabel" TargetType="Label">
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource Tertiary}" />
            </Style>
            <Style x:Key="hiddenLabel" TargetType="Label">
                <Setter Property="IsVisible" Value="False" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <VerticalStackLayout>

        <!-- Legend -->
        <FlexLayout JustifyContent="Center" Wrap="Wrap" Padding="20">
            <HorizontalStackLayout Margin="8,0">
                <BoxView Color="GreenYellow" 
                         WidthRequest="14" 
                         HeightRequest="14" 
                         CornerRadius="3" 
                         VerticalOptions="Center" />
                <Label Text=" Office" FontSize="12" VerticalTextAlignment="Center" />
            </HorizontalStackLayout>

            <HorizontalStackLayout Margin="8,0">
                <BoxView Color="GhostWhite" 
                         WidthRequest="14" 
                         HeightRequest="14" 
                         CornerRadius="3" 
                         VerticalOptions="Center" />
                <Label Text=" Remote/OoO" FontSize="12" VerticalTextAlignment="Center" />
            </HorizontalStackLayout>
        </FlexLayout>
        
        <Border StrokeShape="50">
            <controls:Calendar
                BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                VerticalOptions="Fill"
                HorizontalOptions="Fill"
                FirstDayOfWeek="Monday"
                SwipeUpToHideEnabled="False"
                
                EventIndicatorColor="Red"
                EventIndicatorSelectedColor="White"
                DeselectedDayTextColor="Blue"
                OtherMonthDayColor="Gray"
                SelectedDayTextColor="Cyan"
                SelectedDayBackgroundColor="DarkCyan"
                SelectedTodayTextColor="Green"
                TodayOutlineColor="Blue"
                TodayFillColor="Silver"
                TodayTextColor="Yellow"
                OtherMonthSelectedDayColor="HotPink"
                
                MonthLabelStyle="{StaticResource calendarLabel}"
                YearLabelStyle="{StaticResource calendarLabel}"
                SelectedDateLabelStyle="{StaticResource hiddenLabel}"
                
                Events="{Binding Events}">
                <controls:Calendar.EventTemplate>
                    <DataTemplate>
                        <StackLayout
                            Padding="15,0,0,0">
                            <Label
                                Text="{Binding Name}"
                                FontAttributes="Bold"
                                FontSize="Medium" />
                            <Label
                                Text="{Binding Description}"
                                FontSize="Small"
                                LineBreakMode="WordWrap" />
                        </StackLayout>
                    </DataTemplate>
                </controls:Calendar.EventTemplate>
            </controls:Calendar>
        </Border>
        <DatePicker>
            
        </DatePicker>

    </VerticalStackLayout>
    
</ContentPage>